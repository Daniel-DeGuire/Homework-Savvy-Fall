// ? TO DO: create a for loop that TOTALS the numbers from 1 to 100

let sum = 0;
for (let i = 1; i <= 100; i++) { // runs 100 times, with values of steps 1 through 100
    sum += i;  // adds the right value to the operant sum = sum + i(operant).
console.log("increment", i);
console.log("sum", sum);
};
console.log(sum);

// ? TO DO: create a for loop that TOTALS the numbers from 1 to 100 (remainder)

let total = 0;
for (let i = 1; i < 100; i++) {
    if (i % 2 == 0) {
      console.log("Number is even:" + i)
  
    } 
  }
 
